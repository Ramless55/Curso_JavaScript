const min = (list) => {
    return Math.min(...list);
}

const max = (list) =>{
    return Math.max(...list);
}

//! Mejor Solucion:

//TODO: const min = (list) => Math.min(...list);
//TODO: const max = (list) => Math.max(...list);
